{
  "name": "create-comment-action",
  "version": "0.0.1",
  "description": "GitHub action to create a commit comment either on open PRs that contain the commit or on the commit itself if there are none",
  "main": "index.ts",
  "repository": "https://github.com/shareup/create-comment-action",
  "author": "Nathan Herald <nathan@shareup.app>",
  "license": "MIT",
  "standard": {
    "ignore": [
      "dist"
    ]
  },
  "scripts": {
    "lint": "standard",
    "package": "ncc build index.ts -o dist -s"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.21.0",
    "standard": "^14.3.1"
  },
  "dependencies": {
    "@actions/core": "^1.2.1",
    "@actions/github": "^2.0.1",
    "@octokit/core": "^2.1.2",
    "@octokit/types": "^2.0.2"
  }
}
